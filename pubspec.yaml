name: dspec
description: BDD in Dart, inspired from Rspec, fork of cSpec
version: 0.1.2
repository: https://github.com/Oblivious-Oblivious/dspec

environment:
  sdk: ^3.0.2

dependencies:
  collection: ^1.17.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
